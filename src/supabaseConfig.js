// DailyTrack - Supabase Credentials
// Project: DAILYTRACK | Region: Asia-Pacific
export const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;

// Publishable/Anon key - used for all API calls (Data + Auth)
export const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

// Same key used for auth endpoints (Supabase accepts publishable key for auth too)
export const SUPABASE_JWT_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;
